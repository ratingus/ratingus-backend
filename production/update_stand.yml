---

- name: Update Docker Compose containers
  hosts: ratingus
  gather_facts: no
  become: yes

  tasks:
    - name: Update Docker Compose containers
      community.docker.docker_compose:
        project_src: "{{ project_src }}"
        state: present
        pull: yes
